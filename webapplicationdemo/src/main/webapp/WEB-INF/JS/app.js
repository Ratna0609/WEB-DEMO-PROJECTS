/**
 * npm install webpack -g ---globally
 npm install webpack --save-dev
 
 var config = {
  //webpack configuration goes here.
}
module.exports = config; //webpack.config.js

 */
 var product = calculateProduct(2,3);
var power = calculatePower(2,3);
console.log("Product of two numbers are ", product);
console.log("Power of the given number is ",power);